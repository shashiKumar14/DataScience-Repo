from dotenv import load_dotenv
load_dotenv()

#we have to use USERNAME , PASSWORD variable in the code
USERNAME = os.environ['USER_NAME']
PASSWORD = os.environ['PASS_WORD']

#In .env file
USER_NAME = "username"
PASS_WORD = "password"
